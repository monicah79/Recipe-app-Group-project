<%
=begin%>
 <%= javascript_importmap_tags %> 
<%
=end%>

<p style="color: green"><%= notice %></p>

<h1>Inventories</h1>

<div id="inventories">
  <% @inventories.each do |inventory| %>
    <%= link_to inventory_path(inventory) do %>
      <%= render inventory %>       
    <% end %>
    <%= button_to "Remove", inventory, method: :delete %>
  <% end %>
</div>

<%= link_to "New inventory", new_inventory_path %>
